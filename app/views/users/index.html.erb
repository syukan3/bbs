<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<br><br>
<p>All User Name</p>
<% @users.each do |user| %>
  <% if user.name != "Guest" %>
    <%= link_to(user.name, user_path(user.id)) %>
    <br>
  <% end %>
<% end %>
